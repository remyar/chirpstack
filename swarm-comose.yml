version: '3.6'
networks:
  chirpstack:
    driver: overlay
    attachable: true
services:
  mosquitto:
  image: 127.0.0.1:5000/mosquitto
    ports:
      - 1883:1883
      - 9001:9001
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == manager]
    networks:
        - chirpstack
